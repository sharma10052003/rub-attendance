class Document:
	"""Stub base class — enough for `class X(Document): ...` to import
	cleanly. Real behavior (save/insert/db access) needs a real bench."""

	def __init__(self, *args, **kwargs):
		pass
