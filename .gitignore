.claude/
__pycache__/
*.pyc
.DS_Store

# Portable interpreter used only to run pure-function tests locally on this
# machine (no bench/WSL/Docker here) — not part of the app, never commit it.
/tools/python312/
/tools/python-embed.zip
/tools/get-pip.py

# Portable GitHub CLI, used only so this session can read Actions run logs
# (auth token lives in the OS keyring, not in this repo either way).
/tools/gh.zip
/tools/gh_extracted/
/tools/gh_login.log
